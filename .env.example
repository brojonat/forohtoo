# Server configuration
SERVER_ADDR=:8080
LOG_LEVEL=info

# Database connection string (REQUIRED)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/forohtoo?sslmode=disable

# Test database connection string (for tests only)
TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5433/forohtoo_test?sslmode=disable

# NATS connection string
NATS_URL=nats://localhost:4222

# Solana RPC endpoint (REQUIRED)
# For development, you can use:
# - Public endpoint: https://api.mainnet-beta.solana.com
# - Devnet: https://api.devnet.solana.com
# For production, use a premium RPC provider (include API key in URL):
# - Helius: https://mainnet.helius-rpc.com/?api-key=YOUR-KEY
# - QuickNode: https://YOUR-ENDPOINT.quiknode.pro/YOUR-KEY/
# - Alchemy: https://solana-mainnet.g.alchemy.com/v2/YOUR-KEY
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Temporal server configuration
TEMPORAL_HOST=localhost:7233
# Temporal namespace (use "default" for local development)
TEMPORAL_NAMESPACE=default
# Task queue name for wallet polling workflows
TEMPORAL_TASK_QUEUE=forohtoo-wallet-polling

# Polling configuration
# How often to poll wallets by default
DEFAULT_POLL_INTERVAL=30s
# Minimum allowed poll interval (prevents rate limit abuse)
MIN_POLL_INTERVAL=10s
